on:
  workflow_dispatch:


jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test warning in JS
        uses: actions/github-script@v5
        with:
          script: |
            console.log('::warning ::test warning');
            
